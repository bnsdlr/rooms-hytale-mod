package de.bsdlr.rooms.lib.room.block;

import org.junit.jupiter.api.Test;

import java.util.HashMap;
import java.util.Map;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class RoomBlockTypeTest {
    @Test
    void testGetCount() {
/*        RoomBlockType type1 = new RoomBlockType() {{
            this.blockIdPattern = "Rock_Stone";
            this.andOrs = new RoomBlockType[]{new RoomBlockType() {{
                this.blockIdPattern = "Rock_Bedrock";
            }}};
        }};
        Map<String, Integer> map = new HashMap<>();
        map.put("Rock_Stone", 3);
        map.put("Rock_Bedrock", 3);
        System.out.println("<askldfsad<lkj " + RoomBlockType.getCount(type1, map));
        assertEquals(6, RoomBlockType.getCount(type1, map));*/
    }
}
